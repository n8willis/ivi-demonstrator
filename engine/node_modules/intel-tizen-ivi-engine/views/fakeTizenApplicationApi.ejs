<% include fakeTizenApi %>

application.prototype.addAppInfoEventListener = function(callbacks) {
	this.__addApplicationInstalledListener(function(aResponse) {
		if (!!callbacks.oninstalled)
			callbacks.oninstalled(aResponse);
	});
}